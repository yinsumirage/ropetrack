# Experiments

Use one folder per runnable experiment:

```text
experiments/YYYYMMDD_short_name/
  README.md
  config.yaml
  commands.ps1
  results.md
```

Put lessons and failures in `experience/`; put runnable configs and commands
here.
