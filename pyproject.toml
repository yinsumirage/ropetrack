[project]
name = "ropetrack"
version = "0.1.0"
description = "Bench-first wrist hand tracking repo with rope-distance experiments."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
