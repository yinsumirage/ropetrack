# Legacy experiment entrypoints

`temporal/` preserves the stopped temporal, state, and visibility-gate
experiments needed to reproduce their recorded negative results. They are not
templates for current work and may import one another internally.

Do not add new active code here. Reusable fixes belong in `ropetrack/`; a new
supported CLI belongs in the relevant non-legacy `scripts/` category.
