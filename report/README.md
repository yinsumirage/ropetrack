# Local Report Outputs

This folder is for generated presentation artifacts such as HTML, slides, PDFs,
and temporary export assets derived from `docs/`.

Keep source-of-truth writing in `docs/` and experiment notes in `experience/`.
Generated report files in this folder are ignored by git by default.
